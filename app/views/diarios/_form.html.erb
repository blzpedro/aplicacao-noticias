<%= form_with(model: diario, local: true) do |form| %>
  <% if diario.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(diario.errors.count, "error") %> prohibited this diario from being saved:</h2>

      <ul>
      <% diario.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :titulo %>
    <%= form.text_field :titulo %>
  </div>

  <div class="field">
    <%= form.label :corpo %>
    <%= form.text_field :corpo %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
